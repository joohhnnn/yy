package com.txznet.txz.component.choice;

public interface IReport {

	String getReportId();

	/**
	 * 
	 * @param bSelected
	 *            是否属于选中
	 * @param selectType
	 *            出口操作类型
	 * @param fromVoice
	 *            用户说的话
	 */
	void doReportSelectFinish(boolean bSelected, int selectType, String fromVoice);
}