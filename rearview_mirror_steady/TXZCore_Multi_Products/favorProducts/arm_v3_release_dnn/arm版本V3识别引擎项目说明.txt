1、使用云知声V3离线识别引擎(必须使用x86版本的SDK即so,jar以及TTS模型文件)，可以降低CPU占用。
2、55版本(V3引擎)调试的时候，唤醒暂时不能设置大小模型，设置会报错。(与TXZ2.0版本只有一行代码不一样)注释	//mSpeechUnderstander.setOption(SpeechConstants.ASR_OPT_WAKEUP_MODEL_ID, wakeup_model_id);
3、55版本(V3引擎)调试的时候，更新离线识别词表的时候，不能包含'<'或者'>',否则离线引擎会崩溃。(TXZ2.0版本的代码已经规避)
4、55版本(V3引擎)必须使用V3引擎编译的词表即txz.dat不一样。
5、55版本(V3引擎)回调的唤醒词中包含空格，必须去掉。

和代码规则：
1、把TXZ2.0的稳定版本全部覆盖本项目的代码。
2、然后替换以上不同的地方。